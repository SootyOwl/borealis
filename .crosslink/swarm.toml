[trust]
model = "local-only"
description = "Single-user local tool — no network auth required"

[ignore]
patterns = [
    "authentication",
    "authorization",
    "session management",
]
reason = "Local-only tool with no network exposure"

[boundaries]
external = [
    "cli",
    "file",
]
internal = []
